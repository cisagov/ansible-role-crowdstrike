---
- name: Converge
  hosts: all
  tasks:
    - name: Include ansible-role-crowdstrike
      ansible.builtin.include_role:
        name: ansible-role-crowdstrike
      vars:
        crowdstrike_third_party_bucket_name: "{{ lookup('aws_ssm', '/third_party_bucket_name') }}"
